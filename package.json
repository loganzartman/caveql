{
  "name": "caveql-project",
  "version": "0.0.1",
  "private": true,
  "description": "",
  "keywords": [],
  "author": "Logan Zartman",
  "license": "MPL-2.0",
  "type": "module",
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "dev": "pnpm run --recursive --parallel dev",
    "build": "pnpm run --recursive build",
    "clean": "pnpm run --recursive clean",
    "test": "node --import tsx --test **/*.test.ts",
    "check": "biome check",
    "check:fix": "biome check --write",
    "types": "tsc --noEmit",
    "bench": "node --import tsx lib/benchmarks/parser.bench.ts",
    "ci": "pnpm build && pnpm test && pnpm types && pnpm check && pnpm bench",
    "ci:publish": "pnpm publish -r --access public"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.6",
    "@changesets/cli": "^2.29.5",
    "@parcel/config-default": "^2.15.4",
    "@parcel/packager-ts": "2.15.4",
    "@parcel/transformer-svg-jsx": "^2.15.4",
    "@parcel/transformer-typescript-types": "2.15.4",
    "@types/node": "^24.0.6",
    "buffer": "^6.0.3",
    "mitata": "^1.0.34",
    "parcel": "^2.15.4",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}
